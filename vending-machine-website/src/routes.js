// routes.js
// this file has all the routes, so we can call routes.home or routes.menu 
// also if we want to change the / path we can change it here and it will be reflected in all the files
const routes = {
    home: '/',
    menu: '/menu',
    buttonRef: '/buttonref',
  };
  
  export default routes;
  